객체지향은 3가지 의미가 있다
1 클래스
2 프로토타입
3 기타

메모리를 차지하면 객체로 보는 경향이 있다.

객체 리터럴은 키와 값 > 묶는것

모든 정보(데이터)는 구조를 가진다

객체-속성이있다(attribute,proderty)-속성값

브라우저가 어떻게 동작하는지 설명..


*함수
모든 함수는 리턴을 한다(결과가 있다)
중괄호{}가 없으면 바로 반환값이 나온다.
중괄호를 넣으면 return 이라는 키워드를 반드시 써야한다.

중괄호는 코드를 여러개 짤때 쓰인다.

모든 함수는 (이름) (입력) (출력) 3가지형태로 구성된다.

*void = java에서 함수 입력값에 "hello world"를 쓰면 return값 없이 바로 출력 되기 때문에 억지로 만들어 낸 키워드
-void는 메모리 공간을 차지하지 않는다.


function 이름(o.o)
    파라미터  ↗

함수와 변수는 같은 것

return 이 반환하기 위해 사용된다고 하면 반드시 반환을 해야한다.
하지만
return은 사실 "실행의 제어권을 반납한다는 뜻이다" ("나는 끝났다" 라는 뜻)

java는 로딩을 class로 감싸야 하기 때문에 Main class를 만들고 그 안에 또 main class를 만들어서 쓴다.

배열(arr)안에 데이터가 들어감.
function (getarea)

------------------------------------------------------------------------

> 괄호열고(   )괄호닫고 의 의미는 호출 > 별도의 메모리 공간에서 실행 (stack이라고 함)
스태틱이 없으면 객체에 달라붙는다 스태틱이 있으면 클래스에 달라붙음

*함수를 실행하면 메모리 공간이 생긴다

함수의 이름이 곧 변수의 이름 타입이라는건 변수를 새로 만들때 쓰임
함수는 로직을 재사용하기위해 만듬


함수의 지역변수는 함수가 실행되는 동안에만 존재한다.
각 함수가 호출되어 실행될 때 만들어지고, 함수의 실행이 끝나면 모두 삭제된다.
그래서 지역변수는 그 변수가 속한 함수의 밖이나 다른 함수에서는 부를 수 없다. 매개변수도 함수 안에 정의되므로 지역변수다.

요약) 함수는 로직을 위해 코드를 묶어둔 덩어리다
그래서 변수로 할당하는데 함수의 이름이다.
함수는 ()실행을 하게 되는데 메모리 공간을 쓰게 되고 필요한 변수들이 생긴다.
끝나면 사라지는데 이것을 지역변수라고 한다.

----------------------------------------------------------------

자바스크립트의 함수는 1급 객체이다 (first class)
배열에 넣을 수 있다
다른 함수의 파라미터가 될 수 있다
다른 함수의 리턴 값이 될 수도 있다

변수가 들어가는 곳엔 함수가 들어갈 수 있다.

간단한 작업은 함수보단 연산자를 쓰는게 더 빠르다.


java는 메소드를 메소드로 선언할 수 없다

자바의 클래스는 데이터와 로직이 흩어지지않게 결합

클로저는 돼지저금통이라고 이해하기
*감춰져 있는 참조값이라고 한다. (지역변수가 없어져야 하는데 쓰는 애가 계속 남아서 참조해서 사라지지 못하는 것)

-------------------------------------------------

브라우저에는 3개의 엔진이 동작한다
1구조(html) 2


html=구조를 표현한 언어

개발자 입장에서 html을 바라보자
alert창 하나에도 많은 것이 담겨있다